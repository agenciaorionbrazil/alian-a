export function canSharePrivateResponse({ ownerId, currentUserId }: { ownerId: string; currentUserId: string }) {
  return ownerId === currentUserId;
}

export function assertNeverExposePartnerPrivateData() {
  return true;
}
